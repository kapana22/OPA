/**
 * `Screen`-ის ორეული. `expo-keep-awake` `react-native`-ს ითრევს.
 * ეკრანის ჩაქრობას ტესტი ისედაც ვერ ამოწმებს — მნიშვნელოვანია მხოლოდ,
 * რომ ძრავის კოდი უცვლელი დარჩეს.
 */
export const Screen = {
  keepAwake(): void {},
  release(): void {},
};
